"""Package data: bundled JSON resources (e.g. Sherlock's site catalogue).

Reading these files at runtime should go through
:func:`importlib.resources.files` to stay compatible with zipped wheels.
"""
